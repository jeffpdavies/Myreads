import React from 'react'

class searchbutton extends React.Component {
	render() {
	 return { 

<div className="open-search">
              <a onClick={() => this.setState({ showSearchPage: true })}>
              Add a book
            </div>
            </a>
          

        };
       }
      }
      export deafult searchbutton;
